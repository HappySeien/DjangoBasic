Важно! Приложите к этому уроку ссылку на работающий в развёрнутом состоянии проект.

1. Подготовьте конфигурацию проекта для запуска на развёрнутом сервере.

-- Выполнено

a. (*) Реализуйте отдельную конфигурацию, включающую только настройки для режима
отладки (debug).



2. Подготовьте сервер и разверните на нём проект.



a. (*) При переносе зависимостей реализуйте отдельный файл, включающий пакеты
только для среды отладки (debug).



3. Проведите нагрузочное тестирование, используя ПО locust, и в комментариях к ПЗ укажите
медиану по времени ответа от сервера для запроса по всем страницам при 250
пользователях.



4. (**) Разверните проект на сервере, используя микросервисную архитектуру.



Для запуска celery
celery -A config worker -l INFO -P eventlet (при локальных тестах под windiws)

В винде нет поддержки локалей, но благо гугл помог закостылил их gettext 0.21 and iconv 1.16 - Binaries for Windows
