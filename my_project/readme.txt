1. Сформируйте в проекте систему шаблонизации.
Создайте базовый шаблон.
На его основе переделайте шаблоны других страниц, используя расширения.
Сделайте основное меню сайта как отдельный включаемый шаблон.

-- Выполнил, все разбито на шаблоны.

2. В основном меню реализуйте подсветку элементов, используя шаблонный тег условия.

-- Выполнил. Блок with использовал для задания короткого имени при обращении

3. На странице новостей организуйте цикл, где выводятся карточки новостей (5 одинаковых карточек).

-- Выполнено.

4. В пагинаторе на странице новостей добавьте возможность перехода по ссылкам с параметром.

-- Выполнено.

5. Создайте собственный шаблонный фильтр для автоматического формирования ссылки типа mailto и примените его на странице контактов.

-- Выполнено.

6. (*) Сформируйте JSON-файл и разместите в нём несколько сгенерированных новостей с заголовками, используя сервисы russiannlp или «Балабоба». 
Затем выведите их в цикле на странице новостей.

-- json подключил, наконецто разобрался что не так было, пусть и криво, но работает. Файл делал ручками, пытался сделать
функцию автогенерации этих новостей, но при попытке установить плагин, у меня отвалился pip при том намертво, пришлось
делать окружение по новой, генерацию новостей проводил в облаке) А какие то сам по сути написал


7. (**) На странице новостей создайте отдельную карточку, где некоторые слова представляют собой ссылку с GET-параметром. 
Сделайте так, чтобы при её обработке на стороне сервера производилось автоматическое перенаправление на любую поисковую систему с запросом из этого параметра.

-- Выполнено.

udp.
Отрезал пагинатор и настроил его на 2 страницы, нашел в документации к django как работать с пагинатором, но еще не доконца разобраля,
пока что это все скорее как заглушка, потом все равно переделывать, но по крайней мере работает.